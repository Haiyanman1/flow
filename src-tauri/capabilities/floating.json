{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "floating",
  "description": "Capability for the compact floating timer window",
  "windows": ["floating"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
